import styled from 'styled-components';

export const GuestListPanelContainer = styled.div`
    text-align: center;
    padding: 1rem;
    background-color: var(--primary-color);
    border-radius: 2rem;
    margin-top: 1rem;
    
    h2.title{
        margin-bottom: 1rem;
    }
`